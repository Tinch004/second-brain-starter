# Reglas aprendidas — update-project-memory

Log real de decisiones de arquitectura del vault: por qué se nombra así, por qué se agrupó así, qué se descartó y por qué. Vive separado de `SKILL.md` porque es historial de consulta ocasional (L3 — cold storage), no algo que haga falta releer en cada operación rutinaria de guardar memoria: se lee antes de proponer un cambio de estructura del vault (ver `AGENTS.md`), no antes de cada sesión.

Cada vez que se resuelve algo genuinamente nuevo (una pregunta que no tenía precedente en este archivo), se agrega acá como regla corta, para no volver a preguntarlo. Formato: fecha, regla, por qué. Estas son las reglas de ejemplo con las que arranca este template — a medida que uses tu propio vault, vas a ir agregando las tuyas, específicas de tu contexto:

- **Ejemplo** — Los módulos/componentes reales de un proyecto (código que existe en el repo) son sub-nodos del grafo, no una lista en texto dentro del `-status.md`. Cada módulo real tiene su propia nota en `<project>/modules/<project>-<modulo>.md`, linkeada desde el `-status.md` del proyecto. Por qué: tratarlos como texto plano los deja invisibles en Graph View y pierde la relación de dependencia entre ellos.
- **Ejemplo** — Cuando un proyecto tiene sub-variantes con estados distintos (ej: un componente "en producción" vs. "en desarrollo"), esa distinción va como etiqueta explícita en la nota del módulo, verificada contra el estado real del repo/carpeta — nunca asumida a partir de la prosa de un README.
- **Ejemplo** — Ningún índice de carpeta se llama `README.md`. Se llama `<Carpeta>-index.md` (ej: `Skills-index.md`, `Projects-index.md`) — un nombre único en todo el vault, igual que las notas de proyecto. Por qué: con `README.md` repetido en cada carpeta, el nombre deja de ser único en el grafo/buscador de Obsidian.
- **Ejemplo** — Las skills en `02-Skills/` se agrupan por dominio en subcarpetas (`02-Skills/<grupo>/`), no en lista plana — cada grupo tiene su propio `Skills-<Grupo>-index.md`, linkeado desde `Skills-index.md`. Cuando se instala una skill nueva: decidir a qué grupo pertenece o si amerita un grupo nuevo — nunca agregarla suelta en el índice general.
- **Ejemplo** — Los índices de carpeta nunca hardcodean cuántos elementos hay ("9 skills", "3 proyectos") — esos números quedan desactualizados apenas se agrega uno más. Se cuenta con la lista real, nunca se escribe un número fijo en el texto.
- **Ejemplo** — Cuando el agente descubre una técnica/capacidad reutilizable en la máquina (no memoria de un proyecto, no una skill formal instalada) se anota en `03-Resources/Tecnicas-agente.md`, no se pierde en el chat.
- **Ejemplo** — El vault tiene su propio repo git (privado) para poder clonarlo en otra máquina. Cada vez que este skill escribe algo real, termina con commit + push automático (paso 9). `.gitignore` excluye `.obsidian/plugins/*/data.json` (API keys/claves privadas por-máquina) — si se agrega un plugin nuevo que guarde secretos en otro archivo, agregarlo a mano al `.gitignore`.
- **Ejemplo** — `git pull` va **primero**, siempre, antes de leer o escribir cualquier nota (paso 0) — no solo al guardar. Esto también se agrega al `AGENTS.md` de cada proyecto y al `AGENTS.md` del vault mismo, como paso 0.
- **Ejemplo** — Se sumó `_raw/` (carpeta a nivel vault, mismo tier que `_templates/`): guarda la fuente cruda inmutable antes de compilarla, la nota compilada referencia de vuelta.
- **Ejemplo** — Cada `<project>-status.md` tiene un frontmatter `id: <project>` congelado al crear el proyecto, que nunca cambia — pero el nombre visible (título, archivo, carpeta) sigue libre de cambiar. Resuelve que un rename rompa referencias externas, sin sacrificar legibilidad (nada de UUIDs visibles).
- **Ejemplo** — Además del audit manual (paso 7), un CI (`.github/workflows/vault-audit.yml`) corre el mismo chequeo de huérfanas/links rotos/nombres inválidos en cada push — cubre también un commit hecho a mano desde Obsidian, sin agente de por medio.
- **Ejemplo** — Pedirle una segunda opinión crítica a otro agente (ej. `codex exec` si usás Claude Code, o al revés) sobre una decisión de arquitectura del vault es una técnica válida antes de asentarla — no hace falta que la pida el usuario.
- **2026-08-16** — Cada proyecto suma `<project>-todos.md` (checkboxes Markdown estándar), separado de `-status.md`. Por qué: permite que Dataview consulte tareas abiertas entre proyectos sin infraestructura nueva; "Próximos pasos" pasa a ser un resumen de 2-3 ítems que apunta a la lista completa, para no inflar `-status.md`.
- **2026-08-16** — Orientarse en un proyecto es carga progresiva: `-status.md` + `-todos.md` primero, seguir a `-context.md`/`-decisions.md`/`modules/` solo si la tarea lo exige. Por qué: evita cargar el log completo de sesiones cuando una pregunta corta ya la contesta el status.
- **2026-08-16** — Las entries de `-decisions.md` llevan `**Estado:**` (arranca `vigente`); superseding es un edit in-place de esa sola línea en la entry vieja, nunca una reescritura. Por qué: sin esto no había forma de distinguir vigente de obsoleto sin leer todo el archivo y adivinar por fecha.
- **2026-08-16** — Antes de escribir `-context.md`/`-status.md`, si el proyecto es repo git, se verifica `git status`/`diff --stat`/`log -1` contra lo que dice la conversación (se salta en silencio si no es git). Por qué: el resumen de una sesión puede no coincidir con lo realmente tocado.
- **2026-08-16** — El CI suma detección de secretos de alta confianza (AWS/private keys/GitHub/Slack/Stripe/asignaciones genéricas) en todo `.md` incluyendo `_raw/`, sin allowlist en v1. Por qué: `_raw/` es la zona de mayor riesgo y un secreto commiteado queda en el historial git para siempre — mejor bloquear en CI que confiar en que no se pegue uno.
- **2026-08-17** — La detección del proyecto actual sigue un orden fijo: `AGENTS.md` del propio repo → contexto de archivo dentro del vault → match de cwd/remote contra `Projects-index.md` → preguntar una sola vez. Por qué: la mayoría de las sesiones ya resuelven esto solas (repo con `AGENTS.md` propio); la regla explícita cubre el caso real que faltaba, trabajar directo sobre el vault sin contexto de archivo (ej. un chat de Obsidian), sin inventar heurísticas nuevas para el caso ya resuelto.
- **2026-08-17** — `<project>-status.md` suma líneas opcionales `Repo:`/`Remote:` bajo el título (ya se usaban a mano en varios proyectos, ahora es convención documentada), y `Projects-index.md` refleja ambos en cada bullet. Por qué: sin esto en un lugar barato de leer, no hay forma de matchear "en qué proyecto estoy" por cwd/remote sin abrir cada `-status.md` — y el remote importa aparte del path porque es el dato que sobrevive a un cambio de máquina (el path local no).
- **2026-08-17** — El `AGENTS.md` de un repo de proyecto se crea/edita desde `_templates/project-agents-template.md` en el paso 1, pero **nunca se commitea automáticamente en ese repo** (a diferencia del vault, que sí se commitea solo en el paso 9). Por qué: el vault es el propio repo de este sistema y auto-commitear ahí es seguro; un repo de código ajeno al vault no — su historial lo maneja el usuario en su flujo normal.
- **2026-08-17** — Trackear un proyecto que ya tiene código/historia real (no uno nuevo desde cero) usa un onboarding acotado: `git log -20`/`remote -v`/`status --short`, README corto o sus primeras ~60 líneas, doc de handoff legado si existe, `ls` de primer nivel + manifest de stack — nunca un escaneo completo del repo. Por qué: el paso 1 original solo contemplaba copiar templates en blanco; sin este caso, trackear un proyecto existente arrancaba con memoria vacía pudiendo reconstruirse con una lectura acotada.
- **2026-08-17** — Los ítems nuevos de `-todos.md` suman fecha de alta al final (`- [ ] texto (2026-08-17)`); los viejos sin fecha no se reescriben en bloque. "Bloqueado" en `-todos.md` es a nivel tarea, "Bloqueado en" de `-status.md` es el resumen de una línea a nivel proyecto que apunta ahí si aplica, no repite el detalle. Por qué: la fecha es la base barata para detectar TODOs estancados sin rediseñar el formato después; la distinción de "Bloqueado" evita que las dos notas digan cosas distintas del mismo bloqueo (ya pasó una vez).
- **2026-08-17** — Esta sección se movió a un archivo separado (`reglas-aprendidas.md`), fuera de `SKILL.md`. Por qué: medido en la práctica, esta sección sola pesaba ~31% de `SKILL.md` (1259 de 4019 palabras) y crece para siempre por ser append-only, pero solo hace falta antes de proponer un cambio de estructura — no en el uso rutinario de guardar memoria de sesión. Separarla es aplicarle a la skill misma la misma regla de capas (L0-L3) que el resto de esta skill le exige al contenido del vault.
- **2026-08-17** — El core deja de asumir que toda unidad de trabajo es un repo de código: `<project>-status.md` suma `type` opcional (default implícito `project`, valor libre — `client`/`campaign`/`product`/etc. sin que la skill necesite conocer su semántica) y 5 relaciones opcionales (`parent`, `depends-on`, `blocked-by`, `related-to`, `supersedes`) en frontmatter. Por qué: preparación deliberada para perfiles futuros (Fase 6) sin construirlos todavía — "el core conoce trabajo, el profile conoce el oficio".
- **2026-08-17** — Las relaciones se escriben como wikilinks **entre comillas** en YAML (`depends-on: ["[[x-status]]"]`), nunca sin comillas — verificado empíricamente que `[[x]]` sin comillas se parsea como una lista anidada, no como texto, y rompe el campo. Por qué: es un error fácil de cometer si no se sabe, y silencioso (no tira error visible al escribir, solo al leer el campo después).
- **2026-08-17** — Cada relación se declara en **una sola dirección canónica**, nunca la inversa (si A `depends-on` B, B no declara nada de vuelta). Los vecinos inversos ("¿qué depende de B?") se derivan por búsqueda/grep sobre el frontmatter de todo el vault, nunca se escriben a mano del otro lado. Por qué: decisión explícita del usuario — evita que las dos direcciones queden desincronizadas (mismo tipo de problema que ya resolvimos con "Bloqueado"/"Bloqueado en" en la Fase 1, aplicado ahora a relaciones).
- **2026-08-17** — Como las relaciones son wikilinks, el chequeo de links rotos que ya corre en `vault-audit.yml` las valida gratis — una relación a algo inexistente falla el build igual que cualquier `[[link]]` roto, sin código nuevo. El CI sí suma un step nuevo, acotado a lo que ese chequeo genérico no puede ver: self-reference (una relación que apunta a su propia nota) y `depends-on` circular directo de 2 nodos (A↔B) — ambos chequeos determinísticos y baratos, probados contra fixtures descartables (caso válido, self-reference, circular) antes de sumarlos a la CI real.
- **2026-08-17** — Deliberadamente diferidos en esta fase: un índice compilado del Work Graph (con el tamaño actual del vault, derivar vecinos por grep ya es barato — se reevalúa si eso deja de ser cierto) y "candidate links" por similitud (es conceptualmente parte del lifecycle `observed`/`candidate`/`confirmed`/`superseded` de Experience Learning, una fase futura — construir una versión ad-hoc ahora arriesga no encajar con ese diseño cuando llegue). Ambos con permiso explícito del propio alcance de la fase para diferirse si no pagaban su complejidad todavía.
- **2026-08-17** — El step de CI "salud del Work Graph" usa `find -print0` + `read -d ''` (nunca `for f in $(find ...)`), igual que los otros dos steps de este mismo `vault-audit.yml` — probado que rompe el job entero (no falla limpio, corta ahí) con un path que tiene espacios, por el word-splitting de `$(...)`. Por qué: es la misma convención que el archivo ya usaba antes de esta fase; el bug era justo no seguirla en el step nuevo.
- **2026-08-17** — El chequeo de self-reference lee **cualquier** wikilink del frontmatter (no solo el de la línea con la clave de la relación), porque las listas YAML en block-style (`depends-on:` seguido de `- "[[x]]"` en líneas indentadas debajo) no matchean un regex que exige la clave y el link en la misma línea — probado que sin esto, un self-reference o un `depends-on` circular escrito en block-style pasaba desapercibido. Por qué: nada en la skill ni en el template prohíbe block-style, así que el chequeo tiene que soportarlo, no asumir que siempre se escribe en una sola línea.
- **2026-08-18** — Fase 4 (Intelligence) implementada con alcance reducido: solo los 3 chequeos determinísticos (TODOs estancados >30 días, status sin actualizar >60 días, decisiones sin `**Estado:**`), agrupados como "Memory health" en `SKILL.md` y en un step nuevo de `vault-audit.yml`. Contradicciones, patrones repetidos entre proyectos, y candidatos a skill/resource quedaron diferidos a Fase 5 (Experience Learning) — decisión del usuario antes de implementar, mismo criterio que "candidate links" en la Fase 3: son juicio semántico, no comparación de fechas, y se solapan con el lifecycle que esa fase todavía no diseñó.
- **2026-08-18** — Los chequeos de "Memory health" son **informativos, nunca hard gate** — a diferencia de todos los steps anteriores de `vault-audit.yml` (huérfanas/rotos/README/secretos/self-reference/circular), este nunca hace `exit 1`. Por qué: decisión explícita del usuario — un TODO viejo o un status desactualizado no es un bug que deba bloquear un push, es una señal para revisar cuando corresponda. La skill tampoco corrige nada solo al ver una señal de estas: la menciona, no la toca.
- **2026-08-18** — El step de "Memory health" arranca con `set +e` / `set +o pipefail` explícito, aunque el resto de `vault-audit.yml` no lo necesita. Por qué: el shell default de Actions corre `run:` con `-e -o pipefail`, y bajo eso un `grep` que no matchea dentro de un `$(...)` mata el script entero — probado que el caso más común y ya documentado (un TODO legacy sin fecha, o un `-status.md` sin línea `Actualizado:`) rompía el step "informativo" a mitad de camino, justo lo contrario de lo que decía. Como el propósito entero de este step es no abortar nunca, desactivar `-e`/`pipefail` para todo el bloque es más simple y robusto que parchear cada pipe individual con `|| true`.
- **2026-08-18** — La fecha de alta de un ítem de `-todos.md` se extrae anclada a `\(YYYY-MM-DD\)$` (entre paréntesis, al final de la línea) — no cualquier fecha suelta en el texto de la tarea. Por qué: evita falsos positivos/negativos si la tarea en sí menciona otra fecha (ej. "Migrar antes del 2026-12-01 (2026-08-08)" — la fecha de alta real es la del final).
- **2026-08-18** — Fase 5 (Experience Learning) implementada sin crear un archivo nuevo para "System Learnings": ese rol ya lo cumplía `reglas-aprendidas.md` desde la Fase 2 (reglas sobre cómo funciona/mantiene el vault mismo). Por qué: crear un archivo paralelo hubiera duplicado exactamente el mismo contenido con otro nombre — el propio spec de la fase pedía no mezclar niveles, no crear un nivel que ya existía.
- **2026-08-18** — `<project>-learnings.md` se crea **de forma perezosa** — recién la primera vez que ese proyecto amerita un learning real (2da aparición de un patrón, evidencia fuerte, o regla explícita del usuario) — a diferencia de `-status.md`/`-todos.md`/`-context.md`/`-decisions.md`, que se crean siempre al trackear un proyecto. Por qué: decisión explícita del usuario — una primera aparición aislada y de bajo impacto ya queda registrada en el log de `-context.md`; forzar un 5to archivo vacío en cada proyecto es ruido, no señal.
- **2026-08-18** — No existe un estado `observed` obligatorio en el lifecycle de un learning — el default al crear una entry es `candidate` (o `confirmed` directo si el usuario fijó una regla explícita). Por qué: decisión explícita del usuario — forzar un estado intermedio formal para algo que ya vive informalmente en `-context.md` agrega un paso sin agregar señal; `observed` queda disponible como estado explícito solo para el caso real que lo justifique.
- **2026-08-18** — Domain/Workstyle learnings quedan en **un solo archivo cada uno** (`03-Resources/Domain-learnings.md`, `03-Resources/Workstyle-learnings.md`), con entries planas (`## fecha — título`) y un campo extra (`**Dominio:**` o `**Scope:** user|team`) en vez de subcarpetas o headings de agrupación de 2 niveles. Por qué: un esquema de 2 niveles (`## dominio` + `### entry`) hubiera roto el chequeo de CI de "`**Estado:**` faltante" (que asume que cada `## ` es una entry) sin reescribirlo; un campo plano logra lo mismo (agrupar/filtrar por dominio o scope) sin ese costo, y es consistente con cómo ya se agrupa todo lo demás en este vault (campos, no jerarquías de carpetas, hasta que el volumen real lo justifique — igual criterio que ya se aplicó para no subcarpetar `02-Skills/` de más).
- **2026-08-18** — La promoción project → domain-learning no duplica el contenido: `Domain-learnings.md` pasa a ser la única fuente del Patrón/Solución vigente, y la entry de origen en `<project>-learnings.md` se edita in-place *solo* en su línea `**Estado:**` (`promovido a domain-learning → [[Domain-learnings#...|título]]`) — mismo mecanismo exacto que el superseding de `-decisions.md` desde la sesión del 16/08, aplicado ahora a un caso nuevo (promoción en vez de reemplazo).
- **2026-08-18** — El chequeo de CI "`**Estado:**` faltante" (Fase 4) se generalizó para escanear también `*-learnings.md`, `Domain-learnings.md` y `Workstyle-learnings.md`, no solo `*-decisions.md` — mismo awk, mismo patrón de entry (`## ` + `**Estado:**`), solo se amplió el `find` a 4 patrones de nombre. Probado con fixtures: una entry sin `**Estado:**` se marca, una con `**Estado:** promovido a...` no se marca (el chequeo valida presencia del campo, no su valor).
- **2026-08-18** — La recuperación en capas suma **L1.5 (learnings relevantes)**, entre L1 y L2, pero **scoped por tipo, nunca los tres juntos por default**: project/domain se buscan en cualquier tarea normal sobre el proyecto (caso por default); workstyle solo cuando la tarea involucra decidir cómo encarar el trabajo o hay incertidumbre de autonomía/preferencia; system solo para tareas sobre el vault mismo (ya cubierto por la regla existente de leer `reglas-aprendidas.md` antes de un cambio de estructura). Por qué: decisión explícita del usuario — cargar los tres tipos siempre por "las dudas" es exactamente lo que la Fase 2 (Token Efficiency) buscó evitar para el resto del contenido; el mismo criterio aplica acá.
- **2026-08-18** — Cero detección semántica automática en esta fase: contradicciones, patrones cross-proyecto y candidatos a skill/resource siguen siendo un **procedimiento que el agente sigue con criterio** (grep antes de crear, preguntar si la evidencia no alcanza), nunca un script de CI. Mismo motivo por el que "candidate links" (Fase 3) sigue sin implementarse: automatizar similitud sin juicio semántico genera ruido, no señal.
- **2026-08-18** — `Domain-learnings.md`/`Workstyle-learnings.md` **no usan `## ` para su heading de "sin entries todavía"** (`**Contenido:** ...` en negrita, no un heading) — encontrado por el sub-agente de revisión: el chequeo de CI extendido de arriba trata cualquier línea `^## ` como el título de una entry que necesita `**Estado:**`, así que un `## Contenido` estructural (mismo patrón que ya usan `Resources-index.md`/`Templates-index.md`) generaba un falso positivo permanente apenas se creaban estos dos archivos vacíos — reproducido antes de corregir. Por qué no ajustar el awk en cambio: hubiera significado enseñarle a distinguir "heading de sección" de "heading de entry" en un archivo que mezcla los dos, más frágil que simplemente no usar `## ` para texto que no es una entry.
- **2026-08-18** — Al insertar el Step 6 nuevo (Learnings) y renumerar 6→7...10→11, quedó una referencia cruzada sin actualizar en el Step 1 (decía "a diferencia del paso 9", el commit del vault pasó a ser el paso 10) — encontrado por el sub-agente de revisión, corregido. Al insertar un step nuevo en medio de la lista, no alcanza con renumerar los `N.` de la lista: hay que grepear todo el archivo por referencias cruzadas a números de paso ("paso N") antes de dar la renumeración por cerrada.
- **2026-08-18** — Fase 6 (Work Profiles) implementada como **documentación pura, sin mecanismo nuevo**: 4 archivos en `_profiles/` (`developer`, `agency-marketing`, `product`, `ops-consulting`), cada uno con `type` típicos del dominio, vocabulario de "Foco actual"/decisión-digna-de-registrarse, y de dónde sale la evidencia sin `git`. Un campo `profile:` opcional (mismo criterio que `type`: libre, sin default) en `-status.md` indica cuál aplica, pero no declararlo no cambia nada. Por qué: el spec original pedía "cada perfil adapta vocabulario, evidencia y estructura, pero NO duplica el core" — la única forma de cumplir ambas cosas a la vez (adaptar sin duplicar) es que el profile sea texto que el agente lee, no una rama de código/plantilla nueva por dominio. Ningún archivo/sección del core cambia de nombre ni de estructura por perfil.
- **2026-08-18** — Los `_profiles/*.md` se consultan de forma puntual (al crear un proyecto de dominio no-código, o ante duda real de vocabulario), nunca los 4 de una — mismo criterio de retrieval scoped que ya se aplicó a L1.5 (Fase 5). No se agregó ningún chequeo nuevo de CI para `_profiles/`: son notas Markdown comunes, ya cubiertas por los chequeos existentes de huérfanas/links rotos.
- **2026-08-18** — Compactación de `SKILL.md`: bajó de 5511 a 3850 palabras (~30%). No fue una fase nueva del plan de 6, sino terminar de aplicar el objetivo de la Fase 2 (Token Efficiency) al archivo que se le había escapado — creció ~80% durante las Fases 3-6 sin que se le volviera a aplicar la misma disciplina que se le aplicó una vez. Mecanismo: (1) las secciones "Unidad de trabajo y relaciones"+"Backlinks y vecinos" se movieron a `work-graph.md` (archivo hermano nuevo, mismo patrón que `reglas-aprendidas.md`), y "Aprendizaje por experiencia" completo a `learnings.md` — `SKILL.md` se queda con un pointer de 3-5 líneas en cada caso; (2) "Memory health" se comprimió a un pointer directo a los comentarios de `vault-audit.yml`, sin duplicar la lógica una tercera vez; (3) en los Steps, se separó instrucción operativa de justificación ("por qué") en los casos donde el "por qué" ya estaba registrado con fecha en este mismo archivo — se sacó la cláusula de justificación duplicada, se dejó la instrucción. Deliberadamente NO se extrajeron a archivo aparte "Perfiles de trabajo" (ya es corto y es más restricción/invariante que referencia) ni la capa de recuperación L0-L3/L1.5 ni git-sync (Steps 0/9/10/11): estos se usan en toda sesión, no son "raros" — sacarlos no ahorra nada y arriesga que un agente se salte la carga por capas si el pointer no se dispara. Tampoco se comprimieron a fondo los Steps 2/8/10/11 (evidencia git, auto-audit, commit, resolución de conflictos): son los que más bugs reales previnieron a lo largo de las 6 fases, y compactarlos de más arriesgaba reintroducir ambigüedades ya pagadas. Verificación antes de dar por cerrado: se cruzó cada entrada fechada de este archivo que describe un bug corregido contra el `SKILL.md` compactado, confirmando que la instrucción operativa que lo arregló seguía presente (aunque la prosa de "por qué" ya no estuviera inline) — ninguna se perdió.
